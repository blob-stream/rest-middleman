{
  "name": "rest-server",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "standard && tape test.js",
    "start": "node lib/prepopulate-db.js && nodemon index.js",
    "prepopulate": "rm state.db && node lib/prepopulate-db.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "datauri": "^1.0.4",
    "flat-file-db": "^1.0.0",
    "restify": "^4.1.1"
  },
  "devDependencies": {
    "nodemon": "^1.10.0",
    "standard": "^7.1.2",
    "tape": "^4.6.0"
  }
}
